# Example nginx chart configuration
# This is a simple example that users can easily modify

# Repository URL (Git repository)
repo_url: https://github.com/kubernetes/ingress-nginx
chart_path: charts/ingress-nginx
ref: helm-chart-4.31.1

# Note: When using Git repositories, use 'ref' instead of 'version'
# The ref can be a branch, tag, or commit hash

# Optional: Override chart metadata
chart_overrides:
  description: "NGINX Ingress Controller for Kubernetes - Example Configuration"
  keywords:
    - ingress
    - nginx
    - kubernetes
    - example
  maintainers:
    - name: "Your Name"
      email: "your.email@example.com"
  home: "https://kubernetes.github.io/ingress-nginx/"
  sources:
    - "https://github.com/kubernetes/ingress-nginx"

# Optional: Override default values
values_overrides:
  # Uncomment and modify these values as needed
  # controller:
  #   replicaCount: 2
  #   resources:
  #     limits:
  #       memory: "512Mi"
  #       cpu: "500m"
  #     requests:
  #       memory: "256Mi"
  #       cpu: "250m"
